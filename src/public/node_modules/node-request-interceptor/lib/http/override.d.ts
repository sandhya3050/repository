import { ModuleOverride } from '../glossary';
/**
 * Overrides native `http` and `https` request issuing functions
 * using a given request interception middleware.
 */
export declare const overrideHttpModule: ModuleOverride;
